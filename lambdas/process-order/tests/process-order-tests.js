'use strict';

const app = require('../process-order.js');
const { expect } = require('chai');
const sinon = require('sinon');

describe('Process Order', () => {
  afterEach(() => {
    sinon.restore();
  });

  before(() => {
  });

  after(() => {
  });
});