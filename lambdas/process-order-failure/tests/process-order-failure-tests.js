'use strict';

const app = require('../process-order-failure.js');
const { expect } = require('chai');
const sinon = require('sinon');

describe('Process order failure', () => {
  afterEach(() => {
    sinon.restore();
  });

  before(() => {
  });

  after(() => {
  });
});