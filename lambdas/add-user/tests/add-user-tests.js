'use strict';

const app = require('../add-user.js');
const { expect } = require('chai');
const sinon = require('sinon');

describe('Add User', () => {


  afterEach(() => {
    sinon.restore();
  });

  before(() => {
  });

  after(() => {
  });
});