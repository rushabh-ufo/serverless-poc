'use strict';

const app = require('../stream-function.js');
const { expect } = require('chai');
const sinon = require('sinon');

describe('Stream Function', () => {
  afterEach(() => {
    sinon.restore();
  });

  before(() => {
  });

  after(() => {
  });
});